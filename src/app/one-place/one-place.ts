import { Component } from '@angular/core';
import { Reveal } from '../reveal';

@Component({
  selector: 'app-one-place',
  imports: [Reveal],
  templateUrl: './one-place.html',
})
export class OnePlace {}
